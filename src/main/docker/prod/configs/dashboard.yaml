apiVersion: 1

providers:
  - name: 'Party service dashboard provider'
    orgId: 1
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards
